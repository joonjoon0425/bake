[project]
name = "bake"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gymnasium[classic,classic-control,full]>=1.3.0",
]
