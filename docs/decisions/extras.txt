Extras에 원소를 새로 넣고 빼고 하는 것이 매우 불편해서 (PPO에서 old-log prob과 adv, ret의 수집 시점이 달라서 Extras에 old_log_prob을 뺴고 추가하고 해야 했었다.)
제네릭으로 받지 말고 새로운 구조제를 만들기로 결정. 타입 키를 이용해서 지정된 타입 키를 주면 해당 타입 키의 연관 타입 Value를 Optional하게 반환한다.

frunk의 HList 등등도 고려해 봤었지만 오히려 문제를 더 복잡하게 만들었었다. 또, 컴파일 타임 정확성이 그렇게까지 필요한 건 아니었기 때문에 (Option으로 반환하게 하면 되므로)
내부에서는 동적으로 관리하되 (Tensor가 rank comptime이라서 어쩔 수 없다.) 타입 키를 이용해서 정해진 것만 받을 수 있게 했다.